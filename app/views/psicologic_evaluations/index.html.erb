
<br>
<div class="col-lg-12">
    <div class="ibox float-e-margins">
        <div class="ibox-title">
            <h5> Test psicologic</h5>
            <div class="ibox-content">
              <div class="table-responsive">
                  <p id="notice"><%= notice %></p>
                        <table class="table table-striped table-bordered table-hover dataTables-example" >
                        <thead>
                        <tr>
                          <th>Observations</th>
                          <th>Recomendations</th>
                          <th>Appli detail</th>
                          <th colspan="1">Action</th>
                        </tr>
                      </thead>

                      <tbody>
                        <% @psicologic_evaluations.each do |psicologic_evaluation| %>
                          <tr>
                            <td><%= psicologic_evaluation.observations %></td>
                            <td><%= psicologic_evaluation.recomendations %></td>
                            <td><%= psicologic_evaluation.appli_detail.state %></td>
                          <td><%= link_to '<i class="fa fa-eye text-info"></i>'.html_safe, psicologic_evaluation %>
                                <%= link_to '<i class="fa fa-edit text-navy"></i>'.html_safe, edit_psicologic_evaluation_path(psicologic_evaluation)  %>
                                <%= link_to '<i class="fa fa-trash text-danger"></i>'.html_safe, psicologic_evaluation, method: :delete, data: { confirm: 'Are you sure?' } %></td>
                          </tr>
                        <% end %>
                    </tbody>
                  </table>
              </div>
        <%= link_to 'Nuevo Test psicologico',  new_psicologic_evaluation_path, class:"btn btn-sm btn-white btn-block" %>
      </div>
    </div>
  </div>
</div>
<br>



<% content_for :javascript do %>

      <script type="text/javascript">
            $(function() {

                $('.dataTables-example').DataTable({
                    pageLength: 25,
                    responsive: true,
                    dom: '<"html5buttons"B>lTfgitp',
                    buttons: [
                        { extend: 'copy'},
                        {extend: 'csv'},
                        {extend: 'excel', title: 'ExampleFile'},
                        {extend: 'pdf', title: 'ExampleFile'},

                        {extend: 'print',
                            customize: function (win){
                                $(win.document.body).addClass('white-bg');
                                $(win.document.body).css('font-size', '10px');

                                $(win.document.body).find('table')
                                        .addClass('compact')
                                        .css('font-size', 'inherit');
                            }
                        }
                    ]

                });
              });
  </script>
<% end %>
